> 使用中文和我对话

## 协作方式（重要）

1. **每个任务做完 git 提交一次**，提交信息写清做了什么，方便我回溯
2. 任务按「简单到难」顺序做
3. 一定需要提交代码 push 或者某些操作终端命令时，自行执行，不用等我确认
4. 需要我确认和回复的，及时停下来询问我

## 项目规范
1. 设计规则：无指定情况下，样式、位置、拖动的规则，参考web-shelf.js
2. 代码质量和维护成本：代码结构合理，样式好看，审美高级，代码质量要高，方便维护。要多考虑是否需要抽离逻辑复用，保证逻辑的一致性
3. 访问速度：不要进行多余的网络请求，保证合理的页面缓存和数据缓存
4. 页面整洁：内部实现逻辑不暴露给用户（如「待上传」「超出 N 字不传给模型」这类提示不写到前台）。